import { useState } from "react"; interface BulkActionButtonProps { selectedCount: number; onDownload: () => Promise<void>; } export default function BulkActionButton({ selectedCount, onDownload }: BulkActionButtonProps) { const [isDownloading, setIsDownloading] = useState(false); const handleClick = async () => { setIsDownloading(true); try { await onDownload(); } finally { setIsDownloading(false); } }; return ( <button onClick={handleClick} disabled={selectedCount === 0 || isDownloading} className="btn btn-primary disabled:opacity-50 disabled:cursor-not-allowed relative min-w-[200px]"> {isDownloading ? ( <div className="flex items-center justify-center space-x-2"> <div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" /> <span>Downloading...</span> </div> ) : ( `Download Selected Trailers${selectedCount > 0 ? ` (${selectedCount})` : ""}` )} </button> ); }
