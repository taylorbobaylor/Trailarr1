import { useState, useEffect } from "react"; interface ToastProps { message: string; type: "success" | "error"; onClose: () => void; } export default function Toast({ message, type, onClose }: ToastProps) { useEffect(() => { const timer = setTimeout(() => { onClose(); }, 5000); return () => clearTimeout(timer); }, []); return ( <div className={`fixed bottom-4 right-4 px-6 py-3 rounded-lg shadow-lg transition-all transform translate-y-0 ${type === "success" ? "bg-green-500" : "bg-theme-red"} text-white`}> {message} </div> ); }
