import { useState } from "react"; import { Check } from "lucide-react"; interface Movie { id: number; title: string; year: number; posterPath?: string; trailerUrl?: string; } interface MovieCardProps { movie: Movie; isSelected: boolean; onSelect: () => void; } export default function MovieCard({ movie, isSelected, onSelect }: MovieCardProps) { const [isHovered, setIsHovered] = useState(false); return ( <div className="relative group" onMouseEnter={() => setIsHovered(true)} onMouseLeave={() => setIsHovered(false)}> <div className={`absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-60 transition-opacity duration-200 ${isSelected ? "bg-opacity-60" : ""}`} /> <div className="relative aspect-[2/3] bg-gray-800 rounded-lg overflow-hidden"> {movie.posterPath ? ( <img src={movie.posterPath} alt={movie.title} className="w-full h-full object-cover" /> ) : ( <div className="w-full h-full flex items-center justify-center bg-theme-dark"> <span className="text-theme-light">{movie.title}</span> </div> )} </div> <button onClick={onSelect} className={`absolute top-2 right-2 w-6 h-6 rounded-full border-2 transition-colors ${isSelected ? "bg-theme-blue border-theme-blue" : "border-white bg-transparent"} ${isHovered || isSelected ? "opacity-100" : "opacity-0"}`}> {isSelected && <Check className="w-4 h-4 text-white absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2" />} </button> <div className="mt-2"> <h3 className="text-theme-light font-medium truncate">{movie.title}</h3> <p className="text-gray-400 text-sm">{movie.year}</p> </div> </div> ); }
